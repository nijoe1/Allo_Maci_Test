/// <reference types="node" />
import { ReadPosition } from 'fs';
/** Reads a specified number of bytes from a file and converts them to a `BigInt`. */
export declare function readBytesFromFile(fd: number, offset: number, length: number, position: ReadPosition): BigInt;
