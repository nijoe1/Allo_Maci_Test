import { type MergeMessagesArgs } from "../utils";
/**
 * Merge the message queue on chain
 * @param MergeMessagesArgs - The arguments for the mergeMessages command
 */
export declare const mergeMessages: ({ pollId, quiet, maciAddress, numQueueOps, signer, }: MergeMessagesArgs) => Promise<void>;
//# sourceMappingURL=mergeMessages.d.ts.map